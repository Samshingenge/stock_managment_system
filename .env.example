# Environment Variables for Stock Management System

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=stock_management
DB_USER=stockuser
DB_PASSWORD=stockpass123
DATABASE_URL=postgresql://stockuser:stockpass123@postgres:5432/stock_management

# Application Security
SECRET_KEY=Y-wE_SPR0M9BsemRnTT2mPTWkcKmgS6qQwfu5mBYcoo
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Settings
DEBUG=true
ENVIRONMENT=development

# CORS Settings
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# API Settings
API_HOST=0.0.0.0
API_PORT=8000

# Frontend Settings
REACT_APP_API_URL=http://localhost:8000/api
REACT_APP_APP_NAME=Stock Management System
REACT_APP_VERSION=1.0.0

# Development Tools
PGADMIN_PORT=5050
PGADMIN_EMAIL=admin@stockmanagement.com
PGADMIN_PASSWORD=admin123

# File Upload Settings
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
UPLOAD_FOLDER=uploads

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Pagination Defaults
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Cache Settings (if Redis is added later)
REDIS_URL=redis://localhost:6379/0
CACHE_TTL=300  # 5 minutes

# Email Settings (for future notifications)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@stockmanagement.com

# Backup Settings
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM
BACKUP_RETENTION_DAYS=30
BACKUP_PATH=/backups

# Security Headers
SECURE_HEADERS=true
HTTPS_ONLY=false  # Set to true in production with SSL

# Rate Limiting (requests per minute)
RATE_LIMIT_GENERAL=100
RATE_LIMIT_AUTH=10
RATE_LIMIT_EXPORT=5

# Development Features
ENABLE_CORS=true
ENABLE_DOCS=true
ENABLE_REDOC=true
ENABLE_MONITORING=false